function pow(a,b){
    return Math.pow(a,b);
}
function round(a){
    return Math.round(a);
}
function ceiling(a){
    return Math.ceil(a);
}
module.exports.pow = pow;
module.exports.round = round;
module.exports.ceiling = ceiling;